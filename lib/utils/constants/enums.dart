enum TextGuapliSizes { small, medium, large }

enum OrderStatus { pending, processing, shipped, delivered, cancelled }

enum PaymentMethos { payPal, googlePlay, applePay, visa, masterCard, creditCard, patstack, razorPay, paytm }
